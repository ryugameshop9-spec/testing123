import Link from "next/link";
export default function ProductCard({p}:{p:any}){return <div className="bg-card p-4 rounded-xl shadow hover:-translate-y-1 transition"><h3 className="font-semibold">{p.name}</h3><p className="text-sm text-gray-300">{p.short_description}</p><p className="mt-2 text-neon font-bold">Rp {p.price.toLocaleString('id-ID')}</p><div className="mt-3 flex gap-2"><Link className="text-xs px-3 py-2 rounded bg-accent" href={`/products/${p.slug}`}>Detail</Link><Link className="text-xs px-3 py-2 rounded bg-green-600" href={`/checkout?slug=${p.slug}`}>Order via WhatsApp</Link></div></div>}
